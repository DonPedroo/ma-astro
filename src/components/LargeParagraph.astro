---
// LargeParagraph.astro

interface Props {
  content: string;
  id: string;
}

const { content, id } = Astro.props;
---

<section id={id} class="min-h-screen w-full flex items-center justify-center bg-[url('/images/grunge.webp')] bg-repeat bg-[size:1024px_1024px] bg-center">
  <h1 class="breaker transform">{content}</h1>
</section>
